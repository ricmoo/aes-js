"use strict";
/*
export interface DataLike {
  length: number;
  [ index: number ]: number;
}

export function checkArray(arg: DataLike, copy?: boolean): Uint8Array {
  if (Array.isArray(arg)) { return new Uint8Array(arg); }

  if (!(arg instanceof Uint8Array)) {
    throw new Error('unsupported array-like object');
  }

  if (copy) { return new Uint8Array(arg); }

  return arg;
}
*/
//# sourceMappingURL=utils.js.map